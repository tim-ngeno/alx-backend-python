#!/usr/bin/env python3
"""
Write a type-annotated function `to_str` that takes a float `n` as
argument and returns the string representation fo the float
"""


def to_str(n: float) -> str:
    """
    Takes a floating point number and returns its string representation

    Args:
        n (float): input floating point argument
    """
    return str(n)
